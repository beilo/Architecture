<resources>

    <!-- Base application theme. -->
    <style name="Base_AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/base_colorPrimary</item>
        <item name="colorPrimaryDark">@color/base_colorPrimaryDark</item>
        <item name="colorAccent">@color/base_colorAccent</item>

    </style>

    <style name="Base_SplashTheme" parent="Base_AppTheme">
        <item name="android:windowBackground">@drawable/base_splash_bg</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>


    <style name="Base_ToolBarStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/base_colorAccent</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textSize">14sp</item>

        <item name="android:popupTheme">@style/ToolbarPopupTheme</item>
        <item name="android:actionMenuTextAppearance">@style/Base_ToolbarMenuTextSize</item>
        <item name="android:theme">@style/Theme.ToolBar.Base</item>
    </style>
    
    <style name="Base_ToolbarMenuTextSize" parent="TextAppearance.AppCompat.Menu">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:colorBackground">#212121</item>
    </style>
    <!-- toolbar弹出菜单样式 -->
    <style name="ToolbarPopupTheme" parent="@style/ThemeOverlay.AppCompat">
        <item name="android:colorBackground">#212121</item>
    </style>

    <!--右边三个点的样式-->
    <style name="Theme.ToolBar.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="actionOverflowButtonStyle">@style/ActionButton.Overflow.Base</item>
    </style>
    <style name="ActionButton.Overflow.Base" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/base_ic_action_overflow</item>
    </style>
</resources>
